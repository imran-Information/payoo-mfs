document.getElementById('btn-login').addEventListener('click', function (event) {
    event.preventDefault();

    // phone number value access
    const phoneNumber = document.getElementById('phone-number').value;






})
